name: Execute On Schedule

on:
  push:
    branches:
      - vless-tuic-hy2
  workflow_dispatch:
  schedule:
    - cron: '*/50 0-3 * * *' 
    - cron: '*/50 9-23 * * *' 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Repository Contents
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # Install latest python
          
      - name: Install Python Packages Requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements
          
      - name: Execute Python Script
        run: python vless-tuic-hy2.py
          
      - name: Commit Files
        run: |
          git config --local user.email "932624033@qq.com"
          git config --local user.name "yyyr-otz"
          git add -A
          git diff-index --quiet HEAD || (git commit -a -m "Updated $(TZ='Asia/Beijing' date +'%Y-%m-%d %H:%M %Z')" --allow-empty)

      - name: Push Changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.ACTIONS_SECRETS_TOKEN }}
          branch: vless-tuic-hy2
       
      - name: Clean Up Files
        run: |
          git checkout --orphan latest_branch
          git rm -r -f __pycache__
          git config --local user.email "932624033@qq.com"
          git config --local user.name "yyyr-otz"
          git add -A
          git commit -am "Updated $(TZ='Asia/Beijing' date +'%Y-%m-%d %H:%M %Z')"
          git branch -D vless-tuic-hy2
          git branch -m vless-tuic-hy2
          git push -f origin vless-tuic-hy2

      - name: Push Changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.ACTIONS_SECRETS_TOKEN }}
          branch: vless-tuic-hy2
